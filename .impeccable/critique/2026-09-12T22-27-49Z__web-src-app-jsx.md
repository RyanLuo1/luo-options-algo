---
target: screener page (/app), rebuilt
total_score: 29
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
target_identity: "file:/Users/binkmaster/Desktop/Luo Capital/web/src/App.jsx"
target_fingerprint: "sha256:dd02b892fc280ff3d7d9e47fad5bf26c10ce7a4a791b376babb862b8b145992b"
target_path: /Users/binkmaster/Desktop/Luo Capital/web/src/App.jsx
timestamp: 2026-09-12T22-27-49Z
slug: web-src-app-jsx
---
Method: dual-agent (A: design review subagent · B: detector + Playwright browser evidence subagent), re-run on the rebuilt Screener (`/app`, v1 design system), then one fix round scored by A in a verdict pass. Baseline: 17/40 (2026-09-12 morning).

## Design Health Score (after the fix round)

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 3 | Chip counts stay at their old values while a rescan is in flight. |
| 2 | Match System / Real World | 3 | "Collateral" is the cash-secured figure (now captioned); "/ct" is not a trader abbreviation. |
| 3 | User Control and Freedom | 3 | A removed chip can't be restored without a rescan; Save has no undo. |
| 4 | Consistency and Standards | 3 | Rows are a roving-focus container, not role=grid; selection changes aren't announced. |
| 5 | Error Prevention | 3 | ⌘Enter gated on validity, Save idempotent, zero-count chips inert; still no undo. |
| 6 | Recognition Rather Than Recall | 3 | `/` to focus tickers is undiscoverable; the metric bar is now footnoted. |
| 7 | Flexibility and Efficiency | 3 | Real keyboard model; no Save shortcut, single-ticker filter, no Ticker sort, unmemoized sort. |
| 8 | Aesthetic and Minimalist Design | 3 | At the 1100 floor the table drops three fragments and the panel stacks; "borderline" on most rows (50% default vs a 50–55% strategy). |
| 9 | Error Recovery | 3 | Humanized, dismissable, results kept; skipped reasons lumped (API gives a flat list). |
| 10 | Help and Documentation | 2 | Nothing names or links the strategy explainer; "⌘ Enter" hint is Mac-only. |
| **Total** | | **29/40** | **Good** (baseline 17/40 Poor) |

## The four baseline P1s
1. Table cannot explain its ranking — **CLOSED** (Rank · Ticker · Expires with DTE · Put/Call/Call · Credit/ct · Max profit/ct + metric bar · P(max) · Collateral; the hidden score rendered as the bar and footnoted).
2. Rescans and errors destroy the working view — **CLOSED** (results stay mounted, dimmed, scrollable; progress strip; dismissable error strip; sort survives the same scan context).
3. Per-share vs per-contract mixed — **CLOSED** (per-contract everywhere with "/ct"; "Minimum credit · $ per contract" converts at the boundary; leg tiles say ask/bid).
4. Save unguarded — **CLOSED** after the fix round (ink confirm-role button, risk framing above it, idempotent per result_id with the saved state persisted and rendered as "Saved · View Tradebook").

## Design Specificity Verdict
Authored for this product: the pay/collect convention runs from leg tiles into the curve's point fills and strike labels; strikes read Put / Call / Call; the worst case is a sentence in dollars; the min-credit helper names the $5/share rule. Residue: skeleton-bar teasers read as loading.

Deterministic scan: 0 findings, 0 advisories on App.jsx, components/lc, index.css. Browser facts (Playwright, 1440 and 1100): no page overflow; 0 unnamed controls; all SVGs labelled or hidden; tablist with aria-selected; contrast offenders 1 (zero-count badge, fixed to ink-2); title "web" (fixed); slider thumbs lacked a focus ring (fixed).

## Shape compliance
All four ★ refinements compliant (rank default with "Back to ranked" override keyed on scan context; MetricBar as metric fn + label props; "Minimum credit · $ per contract" default $500 with per-share conversion; sub-1100 out of scope). Deviations resolved in the fix round: color-only narrow zones (legend row), breakpoint re-keyed to the 1100 floor (inclusive: the trimmed layout is what lets 1100 hold without horizontal scroll), ⌘Enter validity gate, tablist keyboard.

## Strengths
- The detail panel is a real trade card: legs with their transactable side, payoff curve with spot and breakeven, four labelled zones, worst case in dollars.
- State handling is correct: render-time reset keyed on scan context; dim not unmount; sort survives rescans.
- The copy does work on every line.

## Remaining items (recorded, not blocking)
- P3 rows as role=grid with aria-activedescendant so ↑/↓ selection is announced.
- P3 Ticker column sort; chip restore without rescan; Save keyboard shortcut; memoize the sort.
- Product decision: the "borderline" pill marks most rows because the strategy's P(max) is structurally 50–55% against the 50% default.
- Server-side unique constraint on (user_id, scan_id, result_id) as defense in depth for Save.
- Skipped-ticker reasons and per-ticker earnings flags and progress need `/api/run` additions.
- Follow-up design problem: sub-1100 widths.

## Persona red flags (post-fix)
Alex: `/` and double-click-to-edit undocumented; no Save shortcut; one ticker filterable at a time. Sam: selection changes silent; 50 bar `role="img"` announcements per page. Riley: 100 tickers wrap the chip row to four lines. Priya: nothing names the strategy or links the explainer; "⌘ Enter" on Windows.
