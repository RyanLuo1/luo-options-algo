---
target: screener page (/app), dual-gate + grouping
total_score: 35
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
target_identity: "file:/Users/binkmaster/Desktop/Luo Capital/web/src/App.jsx"
target_fingerprint: "sha256:efb3e36b09f7d8bc74255adc68f8c30ffd461ade4bce360ab4aedbe0db2d4e57"
target_path: /Users/binkmaster/Desktop/Luo Capital/web/src/App.jsx
timestamp: 2026-09-13T03-53-11Z
slug: web-src-app-jsx
---
Method: dual-agent (A: design review subagent · B: detector + Playwright browser evidence subagent), re-run on the Screener (`/app`) after the change round: borderline pill removed, dual-gate credit defaults ($100 per contract friction floor + 1% return on collateral, the latter applied client-side and live), best-per-ticker grouping with "+N more" expanders and a flat toggle, chip causes from a minimal API addition (`ticker_reasons`), tab-strip pills dropped. A's first pass scored 30/40; a fix round (grouped heads always the scanner-best under a sort; ROC shown wherever it is gated; precise counted zero reasons; controls-bar alignment) was scored by A in a verdict pass at **35/40**, with the controls bar the one partial — subsequently rebuilt as a three-row grid (label / input / helper) with one-line helpers and ⓘ tooltips, measured: one baseline per band at 1440 and 1100.

## Design Health Score (A's verdict pass)

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 4 | Chip counts and causes; "Ranked by the scanner / Sorted by … / Back to ranked". |
| 2 | Match System / Real World | 4 | ROC displayed wherever it is gated (Credit cell subline, Credit stat caption). |
| 3 | User Control and Freedom | 4 | Heads stable under sort; expansions survive ROC edits; Save has no undo. |
| 4 | Consistency and Standards | 3 | Execution model named per gate; on-screen rank is post-floor, logged rank is not (stated in the caption). |
| 5 | Error Prevention | 3 | Validity gates every run path; no undo. |
| 6 | Recognition Rather Than Recall | 3 | "+N more" says nothing about what varies. |
| 7 | Flexibility and Efficiency | 4 | → / ← expand, ↑/↓, Enter, ⌘Enter, `/`, live ROC. |
| 8 | Aesthetic and Minimalist Design | 3 | Held by the controls bar at verdict time (since rebuilt as a three-row grid). |
| 9 | Error Recovery | 4 | Counted, non-hedging causes; no overclaim. |
| 10 | Help and Documentation | 3 | Explanations live in a long caption and tooltips. |
| **Total** | | **35/40** | **Good** (29 before this round; 17 at the pre-rebuild baseline) |

## Deterministic scan and browser facts (B)
Detector 0 findings, 0 advisories (13 files, 1,711 lines; sanity-checked against a seeded scratch file). Contrast: 141 / 134 text elements checked at 1440 / 1100, 0 below threshold, minimum ratio 4.93. No page overflow at either width; 38 controls all named; 13 graphics all labelled or hidden; tablist with roving tabindex; expanders with aria-expanded; view toggle with aria-pressed; document.title "Luo Capital". Keyboard: 30 Tab stops in a sensible order; ArrowRight expands the selected ticker, ArrowDown moves into its variants, `/` focuses tickers.

## The three changes (A)
1. Borderline pill removed — improvement, cleanly done.
2. Dual-gate default — right idea; the ROC floor gated a number not shown (fixed: shown wherever gated) and the two gates looked identical while behaving differently (fixed: helpers name the model).
3. Best-per-ticker grouping — improves the job at default; under a sort the head followed the sort (fixed: head is always the scanner-best; sort reorders groups and variants). Chip causes hedged (fixed: counted reasons from the API's counters).

## Remaining (recorded, not blocking)
- "+N more" row is not in the ↑/↓ path (→ / ← expand instead); rows are not role=grid with aria-activedescendant.
- Payoff label vs spot-line collision when spot sits inside the keep-credit zone.
- Chips sit outside the controls card; the chip trailer can wrap into a paragraph on a 10-ticker scan with several zeros.
- The 5001 string in the data hook (humanized downstream).
- Product: the 50% P(max) default sits at the strategy's structural 50–55%.
